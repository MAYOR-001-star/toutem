const BlogSection = () => {
    return (
        <section id="blog" className="h-screen bg-gray-500 flex items-center justify-center">
            <h1 className="text-4xl">Blog Section</h1>
        </section>
    );
};

export default BlogSection;
